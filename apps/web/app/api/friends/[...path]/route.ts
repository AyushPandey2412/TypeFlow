import { NextRequest, NextResponse } from "next/server";
import { createFriendInvite, friendStats, listInvites, respondToInvite } from "../../../../../server/src/services/friend.service.js";
import { friendInviteSchema } from "../../../../../server/src/validators/friend.validator.js";
import { apiError, connectDatabase, registeredUserId, requireSameOrigin } from "../../../../lib/server-backend";

type Context = { params: Promise<{ path: string[] }> };
export async function GET(request: NextRequest, context: Context) { try { await connectDatabase(); const { path } = await context.params; const userId = registeredUserId(request); if (path[0] === "invites") return NextResponse.json({ invites: await listInvites(userId) }); if (path[0] && path[1] === "stats") return NextResponse.json(await friendStats(userId, path[0])); return NextResponse.json({ error: "Not found" }, { status: 404 }); } catch (error) { return apiError(error); } }
export async function POST(request: NextRequest, context: Context) { try { requireSameOrigin(request); await connectDatabase(); const { path } = await context.params;const userId=registeredUserId(request);if(path[0]==="invites"&&path[1]&&path[2]&&["accept","reject","cancel"].includes(path[2]))return NextResponse.json(await respondToInvite(userId,path[1],path[2] as "accept"|"reject"|"cancel"));if (path[0] && path[1] === "invite") return NextResponse.json(await createFriendInvite(userId, path[0], friendInviteSchema.parse(await request.json())), { status: 201 }); return NextResponse.json({ error: "Not found" }, { status: 404 }); } catch (error) { return apiError(error); } }
